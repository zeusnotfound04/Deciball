# Host port overrides for the docker-compose stack.
# Copy to .env and change any that collide with other projects on your machine.
# Container-internal ports are fixed; these only affect host bindings.

POSTGRES_PORT=5432
REDIS_PORT_HOST=6379
WEB_PORT=3000
WS_PORT=8080
